name: 8Tech FE Repo Workflow
on:
  pull_request_target:
    branches:
      - master
      - main
jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
